management:
  endpoints:
    web:
      exposure:
        include: "*"
server:
  port: 8086

eureka:
  instance:
    prefer-ip-address: true
    non-secure-port: 8086
    non-secure-port-enabled: true
    ip-address: 127.0.0.1
